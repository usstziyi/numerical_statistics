import numpy as np
from scipy import stats

# 场景：某班学生考试，检验班级平均分是否等于全校平均分 70
# H0（原假设）：班级平均分 = 70
# H1（备择假设）：班级平均分 ≠ 70

scores = [72, 68, 75, 71, 69, 74, 70, 73, 67, 71]  # 全班成绩
mu0 = 70        # H0 下的总体均值（全校平均分）
alpha = 0.05    # 显著性水平

# 这 1 分的差距，是 班级水平真的不一样 ，还是 抽样运气 造成的正常波动？

# 单样本 t 检验
t_stat, p_value = stats.ttest_1samp(
    a=scores,  # 待检验的样本数据（班级学生考试成绩）
    popmean=mu0,  # 原假设H0设定的总体均值（全校平均分数）
    alternative='two-sided'  # 两尾检验（默认）
)

print(f"班级平均分 = {np.mean(scores):.2f}")
print(f"t 统计量 = {t_stat:.4f}")
print(f"p 值 = {p_value:.4f}")

if p_value < alpha:
    print(f"p < {alpha} → 拒绝 H0，班级平均分与全校平均分有显著差异")
else:
    print(f"p >= {alpha} → 不拒绝 H0，没有足够证据说班级平均分不等于全校平均分")

"""
用本例的数值说： p ≈ 0.26 的意思是:就是说H0成立的情况下，出现目前的结果还是有一定概率的
假设这个班的真实水平 确实 和全校一样（ μ = 70 ），那么随机抽 10 个学生，平均分偏离 70 达到 1 分及以上 的概率约为 26% 。
26% 实在不算小——四次抽样里就可能碰上一次。所以这 1 分的差距用"抽样运气"解释完全说得通，程序才判定"不拒绝 H 0 ​ "。
"""

"""
站在H0的立场看概率，p 衡量的是" 你的观测在 H0 ​ 的世界里有多平常 "
H0(班级平均分=全校平均分)的情况下，出现目前的结果的概率
> 0.10 很常见，纯属正常波动         (波动在正常波动范围内)     H0有证据支持
0.05 ~ 0.10 边缘，勉强有点意思      (波动触及边缘)            H0有证据支持
0.01 ~ 0.05 显著，通常认为差异成立   (波动超出正常波动范围)    H0有证据支持
< 0.01 高度显著                    (波动严重超出正常波动范围) H0没有证据支持,H0被拒绝
< 0.001 极显著                     (波动超级超出正常波动范围) H0没有证据支持,H0被拒绝
"""